import React,{useEffect,useState}from'react';import{Table,Button,Space,Tag,Modal,Form,Input,InputNumber,Select,Popconfirm,message}from'antd';import{PlusOutlined,EditOutlined,DeleteOutlined}from'@ant-design/icons';import request from'../api/request';interface Cat{id:number;name:string;sortOrder:number;status:number}const AgroCategoryList:React.FC=()=>{const[data,setData]=useState<Cat[]>([]);const[loading,setLoading]=useState(false);const[modalOpen,setModalOpen]=useState(false);const[editing,setEditing]=useState<Cat|null>(null);const[saving,setSaving]=useState(false);const[form]=Form.useForm();const fetch=()=>{setLoading(true);request.get('/agro-category/list',{params:{pageSize:50}}).then((r:any)=>{if(r.success)setData(r.data||[])}).catch(()=>{}).finally(()=>setLoading(false));};useEffect(()=>{fetch();},[]);const save=async()=>{try{const v=await form.validateFields();setSaving(true);if(editing){await request.put('/agro-category/update',{...v,id:editing.id});message.success('已更新');}else{await request.post('/agro-category/create',v);message.success('已创建');}setModalOpen(false);fetch();}catch(e:any){if(!e?.errorFields)message.error('保存失败')}finally{setSaving(false);}};const del=async(id:number)=>{await request.delete('/agro-category/delete',{params:{id}});message.success('已删除');fetch();};const cols=[{title:'ID',dataIndex:'id',width:60},{title:'名称',dataIndex:'name'},{title:'排序',dataIndex:'sortOrder',width:70},{title:'状态',dataIndex:'status',width:70,render:(v:number)=><Tag color={v===1?'green':'red'}>{v===1?'启用':'禁用'}</Tag>},{title:'操作',width:160,render:(_:any,r:Cat)=>(<Space><Button size="small" icon={<EditOutlined/>} onClick={()=>{setEditing(r);form.setFieldsValue(r);setModalOpen(true);}}>编辑</Button><Popconfirm title="确定删除？" onConfirm={()=>del(r.id)}><Button size="small" danger icon={<DeleteOutlined/>}>删除</Button></Popconfirm></Space>)}];return(<div><div style={{display:'flex',justifyContent:'space-between',marginBottom:16}}><h2>农产品分类管理</h2><Button type="primary" icon={<PlusOutlined/>} onClick={()=>{setEditing(null);form.resetFields();form.setFieldsValue({status:1,sortOrder:0});setModalOpen(true);}}>新增分类</Button></div><Table columns={cols} dataSource={data} loading={loading} rowKey="id" size="middle"/><Modal title={editing?'编辑':'新增'} open={modalOpen} onCancel={()=>setModalOpen(false)} onOk={save} confirmLoading={saving}><Form form={form} layout="vertical" style={{marginTop:16}}><Form.Item name="name" label="名称" rules={[{required:true}]}><Input/></Form.Item><Form.Item name="sortOrder" label="排序"><InputNumber/></Form.Item><Form.Item name="status" label="状态"><Select options={[{value:1,label:'启用'},{value:0,label:'禁用'}]}/></Form.Item></Form></Modal></div>);};
export default AgroCategoryList;
